name: loans
description: "App para gestionar préstamos entre empresas con Supabase + Clean Architecture + BLoC"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ============== STATE MANAGEMENT ==============
  flutter_bloc: ^9.0.0
  hydrated_bloc: ^10.1.1
  equatable: ^2.0.5

  # ============== SUPABASE (Primary Backend) ==============
  supabase_flutter: ^2.5.0
  supabase: ^2.4.0

  # ============== AUTHENTICATION ==============
  google_sign_in: ^6.2.0

  # ============== LOCAL STORAGE ==============
  shared_preferences: ^2.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # ============== NAVIGATION ==============
  go_router: ^14.0.0

  # ============== UI/DESIGN ==============
  flutter_svg: ^2.0.0
  cached_network_image: ^3.3.0
  flutter_slidable: ^3.0.0
  cupertino_icons: ^1.0.8

  # ============== DATA & SERIALIZATION ==============
  json_serializable: ^6.7.0
  json_annotation: ^4.8.0
  dartz: ^0.10.1
  get_it: ^7.6.0

  # ============== UTILITIES ==============
  intl: ^0.19.0
  path_provider: ^2.1.0
  uuid: ^4.0.0
  flutter_dotenv: ^5.1.0
  logging: ^1.1.1

  # ============== REACTIVE ==============
  rxdart: ^0.27.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.4
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - .env
